# Git Proxy

## ON

```shell
git config http.proxy http://ip_address:port
git config https.proxy http://ip_address:port
```

## OFF

```shell
git config --unset http.proxy
git config --unset https.proxy
```